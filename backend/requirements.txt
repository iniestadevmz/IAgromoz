# Minimal dependencies required to run the Django API in production.
# Add extras (e.g., psycopg2-binary) only if your deployment environment requires them.

Django==6.0
django-cors-headers==4.9.0
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1

# Database URL parsing (used in settings.py)
dj-database-url==3.1.2

# Config / env vars
python-decouple==3.8
python-dotenv==1.2.2

# Static file serving (production)
whitenoise==6.12.0

# WSGI server (production)
gunicorn==25.1.0

# Google Gemini (AI) provider
google-genai==1.65.0

psycopg[binary]>=3.1

cloudinary
django-cloudinary-storage
